# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-Energy_Detector
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND Energy_Detector_sources
)

set(Energy_Detector_sources "${Energy_Detector_sources}" PARENT_SCOPE)
if(NOT Energy_Detector_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT Energy_Detector_sources)

add_library(gnuradio-Energy_Detector SHARED ${Energy_Detector_sources})
target_link_libraries(gnuradio-Energy_Detector gnuradio::gnuradio-runtime)
target_include_directories(gnuradio-Energy_Detector
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-Energy_Detector PROPERTIES DEFINE_SYMBOL "gnuradio_Energy_Detector_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-Energy_Detector PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-Energy_Detector)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_Energy_Detector_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-Energy_Detector)

if(NOT test_Energy_Detector_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_Energy_Detector_sources)

foreach(qa_file ${test_Energy_Detector_sources})
    GR_ADD_CPP_TEST("Energy_Detector_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
